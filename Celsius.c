#include <stdio.h>
int main()
{
    float c;
    printf("Enter the value is C = ");
    scanf("%f", &c);
    float result = (c * 9 / 5) + 32;
    printf(" answre   %f", result);

    return 0;
}